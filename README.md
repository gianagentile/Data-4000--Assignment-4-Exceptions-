# Data-4000--Assignment-4-Exceptions-

#1 - salary_data.py 
- this prompt stimulate entering sales data. It prompts for units sold (integer) and unit price (float). It also uses a reuable valiation function, handles invalid numeric input using "ValueError" and caculates total error.


#2 inventory_checher.py 
- this prompt checks inventory lecels for the supply chain management
- it prompts the user inventory and reorder thresholds, handles non interger inputs, handles divivison by zerp when cac. perentages and alerts user if inventory is below threshold

#3- age_verfication.py
- this prompt determines whether a cusotmer quailfies for age-restricted promotions
- it uses input validation loop, handles invalid integers, uses "NameError" to intentionally refrence an undefined variable, and automatically fixes the error to continue

#4- finanical_ratio.py 
- this prompt caculates a companys profit margin, it prompts for profitand revenuse as floats, ses "try-except-else" structure, silently repropmts minior errors, handles division by zero, and displays perccntages results when valid.

35- salary_adjustments.py 
- this prompt creates HR salary changes
- it prompts for current salary and adjustment percentages, uses helper validation functiom, handles invalid numeric input, includes custom valdiation for percentage range and demonstrates manual expception rasing. 
